export enum SystemMessage {
    UNKNOWN_ERROR = 'An Error Occurred',
    PERMISSION_DENIED = 'Permission Denied!',
    CONNECTION_ERROR = 'Connection was Interrupted!',
    INVALID_AUTHENTICATION = 'Credentials are Invalid!',
}